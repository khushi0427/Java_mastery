/**
 * A second class, compiled into a separate output directory, so the classpath
 * has something real to find. Module 01, Chapter 1.
 */
public class Greeter {
    public static String greet(String who) {
        return "Hello, " + who + "!";
    }
}
